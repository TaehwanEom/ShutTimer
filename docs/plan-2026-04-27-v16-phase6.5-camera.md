# [v1.6-Phase 6.5] RoutineAlarm 카메라 모드 통합 — 정식 계획서

**작성:** 2026-04-27 (플랜 창)
**작업 트랙:** T0 (사용자 결정 2026-04-27)
**브랜치:** `feature/v1.6-routine`
**기준 HEAD:** `03b700a feat: v1.6 routine 사이클 통합 + 진행 중 루틴 카드 편집/삭제 양방향 차단`
**참고:** [docs/v16-phase6.5-plan-brief-2026-04-27.md](v16-phase6.5-plan-brief-2026-04-27.md) (구현 창 → 플랜 창 인수 자료)

---

## 1. 작업 정의 + 범위

**목표:** RoutineAlarmScreen 의 `endMethod='camera'` 분기에서 v1.5 AlarmCameraMode 의 실제 사진 인식 흐름을 통합. 현재 placeholder (랜덤 emoji + 탭 dismiss) 제거.

**범위 (확정):**
- 단일 화면 통합 — RoutineAlarmScreen 만 수정
- AlarmScreen / AlarmCameraMode / 의존성 / 권한 / 설정 변경 X
- AsyncStorage 신규 키 추가 X (분리 정책)
- v1.5 EAS Dev Build 그대로 재사용 가능 (의존성 추가 0)

**범위 밖 (v1.7+):**
- routine 별 cameraMissions[] 필드
- 별도 미션 선택 UI
- (β) hook 추출 / (γ) self-contained 리팩토링 — **분리 정책에 의해 거부됨**

---

## 2. 통합 방식 — (α) 부모 복제 채택 (사용자 결정 D-1)

### 채택 근거
- 반복실수 #1 (외부 패키지) 위험 최소 — AlarmCameraMode 365L 무수정
- v1.5 출시 기능 보호 — AlarmScreen 미수정
- 사이클 일정 부합 — 1-2일 추정 (progress §7 일치)
- **분리 정책 정합** (사용자 결정 2026-04-27) — AlarmScreen ↔ RoutineAlarmScreen 의도 분리. 코드 중복 허용

### 거부된 옵션
- **(β) hook 추출** — 분리 정책 위반 (양 화면이 동일 hook 의존). 거부.
- **(γ) self-contained 리팩토링** — AlarmCameraMode 변경 + worklet/SharedValue 참조 분리 위험. 거부.

---

## 3. 사용자 결정 사항 (D-1 ~ D-8 확정)

| ID | 항목 | 결정 |
|----|------|------|
| D-1 | 통합 방식 | **(α) 부모 복제** |
| D-2 | 미션 풀 | **(D-2-i) MISSION_POOL 전체 픽** — `selectedMissions` AsyncStorage 키 사용 X (분리 정책) |
| D-3 | dismiss 흐름 | RoutineAlarm 자체 `handleDismiss()` 재사용 |
| D-4 | fallback | 권한 거부 = 탭 dismiss / 시간 초과 = `isDanger` 패턴 / 인식 실패 = 슬롯머신 재돌림 |
| D-5 | 권한 흐름 | vision-camera 권한 자동 재사용 + 미부여 시 fallback UI (탭 dismiss) |
| D-6 | 슬롯머신 / 재돌림 / 사용자 미션 풀 | v1.5 동일 UX |
| D-7 | 콜드 스타트 / kill 후 재시작 | mount 시 매번 fresh init (SharedValue 자체 생성) |
| D-8 | i18n | 사이클 마지막 단계 (사용자 정책 #1) — 호출 코드는 `t('key', { defaultValue: '...' })` 패턴 |

---

## 4. 영향 범위 분석

### 4.1 직접 수정 대상
- [src/screens/RoutineAlarmScreen.tsx](../src/screens/RoutineAlarmScreen.tsx) (단일)

### 4.2 절대 수정 금지 (분리 정책 + v1.5 보호)
- `src/screens/AlarmScreen.tsx` — v1.5 출시 기능 보호 + 분리 정책
- `src/screens/AlarmCameraMode.tsx` — 365L 그대로. props 인터페이스 변경 X
- `src/constants/missionIcons.ts` — import 만, 변경 X
- AsyncStorage `selectedMissions` 키 — read/write X (분리 정책)
- `src/locales/ko.json` / `src/locales/en.json` — i18n 사이클 마지막 (Step E)
- `Info.plist` / `app.json` / `babel.config.js` / `Podfile` — v1.5 설정 그대로 (변경 0)

### 4.3 간접 영향
- AlarmCameraMode 마운트 — 컴포넌트 자체 수정 X. props 매핑만
- vision-camera 권한 — OS 권한 (Info.plist 그대로)

### 4.4 iOS / Android 차이
- 없음 — vision-camera 권한 v1.5 그대로 (Info.plist `NSCameraUsageDescription` + Android `CAMERA`)
- worklet / frame processor — Android 동작 검증 (v1.5 검증 가정)

### 4.5 앱 상태별 동작
- foreground only — RoutineAlarm 진입은 알람 응답이라 항상 foreground
- background 카메라 작동 X (정상)

### 4.6 분리 정책 검증 항목
- AlarmScreen 의 `selectedMissions` state / AsyncStorage / SharedValue 변경 0
- RoutineAlarm 진입 후 AlarmScreen 재진입 시 사용자 미션 선택 그대로 보존

---

## 5. 단계 분할 (Step A ~ E)

각 Step 단독 적용 + tsc 패스 + Cmd+R + 사용자 검증 후 다음 Step 진행.

### Step A — placeholder 제거 + import 추가
**수정:** [RoutineAlarmScreen.tsx#L329-L342](../src/screens/RoutineAlarmScreen.tsx#L329) placeholder 제거 + 헤더 import 추가
- `useSharedValue` from `react-native-worklets-core`
- `AlarmCameraMode` from `./AlarmCameraMode`
- `MISSION_COCO_LABELS`, `MISSION_CONFIDENCE_OVERRIDE` from `../constants/missionIcons` (기존 MISSION_EMOJI/POOL/LABEL 옆)

**검증:**
- `npx tsc --noEmit` EXIT=0
- 카메라 분기 진입 시 빈 화면 (의도된 임시 상태 — Step C 마운트 전)

### Step B — 부모-측 state / SV / 애니메이션 / frame processor 갱신
**수정:** RoutineAlarmScreen.tsx 본문에 부모-측 로직 신규 추가
- SharedValues 6개: `matched`, `lastRun`, `targetLabelsSV`, `thresholdSV`, `consecutiveHits`, `isShufflingSV`
  - 초기값: `targetLabelsSV = MISSION_COCO_LABELS[cameraMission ?? ''] ?? []`, `thresholdSV = MISSION_CONFIDENCE_OVERRIDE[cameraMission ?? ''] ?? TARGET_CONFIDENCE`
- 슬롯머신 state 3개: `isShuffling`, `shuffledList`, `shuffleIdx` (`useState`)
- 슬롯머신 ref 2개: `shuffleIntervalRef`, `shuffleTimeoutRef` (`useRef`)
- UI 상태 / 애니메이션 / 레이아웃 — AlarmCameraMode props 요구 항목 모두 RoutineAlarm 자체 생성
  - `isRetryBannerVisible`, `successAnimating`, `resultState`, `isDanger`, `remainingSeconds` (`useState`)
  - `successBlink`, `successFill`, `scanLine`, `dangerBlink` (`Animated.Value`)
  - `boxWidth`, `boxHeight` (레이아웃 측정)
- frame processor 갱신 useEffect — `cameraMission` 변경 시 `targetLabelsSV.value` + `thresholdSV.value` 재설정 (deps `[cameraMission, targetLabelsSV, thresholdSV]`)

**검증:**
- `npx tsc --noEmit` EXIT=0
- 마운트 에러 X (콘솔 에러 0건)

### Step C — AlarmCameraMode 마운트 + props 매핑
**수정:** Step A 의 빈 분기에 AlarmCameraMode 마운트
- AlarmCameraMode props 인터페이스 정독 ([AlarmCameraMode.tsx#L36-L80](../src/screens/AlarmCameraMode.tsx#L36)) → 매핑 표 작성 → 누락 0 검증
- props 약 25개 매핑

**검증:**
- 시나리오 #1 — endMethod='camera' routine 알람 진입 시 카메라 마운트 + 미션 표시
- iOS + Android 양쪽

### Step D — 슬롯머신 재돌림 + onMatchDetected + fallback
**수정:** RoutineAlarmScreen 본문에 콜백 신규 추가
- `handleReshuffle` — [AlarmScreen.tsx:788-820](../src/screens/AlarmScreen.tsx#L788) 패턴 차용 (코드 별도). **분리 정책: `selectedMissions` 사용 X. `MISSION_POOL` 전체에서 픽**
- `handleMatchDetected` — 인식 성공 시 `handleDismiss()` 재사용 (D-3)
- fallback (D-4):
  - 권한 거부: 탭 dismiss UI (placeholder 흐름 보존)
  - 시간 초과: `isDanger` 패턴 (AlarmScreen 패턴 차용)
  - 인식 실패: 슬롯머신 재돌림

**검증:**
- 시나리오 #2 ~ #7
- 시나리오 #10 분리 검증 — AlarmScreen `selectedMissions` 변경 0 확인

### Step E — i18n 정식 키 등록
**수정:** ko.json (사용자 정책 #1 — ko 만)
- `routine.cameraScanHint` "(임시: 탭하여 계속)" → 정식 문구 (Step E 시점에 사용자와 텍스트 확정)
- 그 외 Step C/D 에서 추가된 `t('key', { defaultValue: '...' })` 호출의 키 정식 등록

**검증:**
- `npx tsc --noEmit` EXIT=0
- 시나리오 #1 i18n 출력 확인

---

## 6. 위험 요소 + 완화

### 6.1 외부 패키지 (반복실수 #1 — Critical)
- **위험:** react-native-fast-tflite / worklets-core / vision-camera-resize-plugin 추측 사용 → 빌드 실패 / 런타임 크래시
- **완화:** AlarmCameraMode 무수정 → 위험 ↓. 단 AlarmCameraMode props 매핑 시 SharedValue 타입 (`ISharedValue<T>`) 정확히 일치 — Step C 인터페이스 정독 + 매핑 표 작성 필수. **타입 정의 (.d.ts) 만 보고 추측 금지** — 공식 사용 예제 (AlarmScreen.tsx 그 자체) 정독

### 6.2 분리 정책 위반 위험
- **위험:** Step C/D 진행 중 무의식적으로 AlarmScreen / `selectedMissions` 참조
- **완화:** Step D 완료 후 시나리오 #10 분리 검증 시점에 `git diff` 로 AlarmScreen 미수정 + `grep -rn "selectedMissions" src/screens/RoutineAlarmScreen.tsx` 결과 0 확인

### 6.3 vision-camera 멀티 마운트
- **위험:** AlarmScreen + RoutineAlarm 동시 마운트 시 카메라 hardware 충돌
- **완화:** navigation stack 으로 한 화면씩 마운트 → 일반적으로 동시 마운트 X. 시나리오 #7 검증

### 6.4 worklet stale closure
- **위험:** mount fresh init 시 frame processor 가 SharedValue 참조 캡처 — mount 마다 frame processor 재생성
- **완화:** AlarmCameraMode 자체가 `useFrameProcessor` 사용 (props SharedValue 직접 참조 — RN worklets-core 권장 패턴). v1.5 검증 상태 그대로 활용

### 6.5 Expo Dev Build 필수
- **위험:** Expo Go 에서 vision-camera / worklets-core / fast-tflite 동작 X — 반복실수 #7 (테스트 환경 없이 구현 금지) 발생 가능
- **완화:** v1.5 Dev Build 그대로 재사용 (의존성 추가 0). 빌드 재생성 불필요

---

## 7. 검증 시나리오 (10건)

| # | 시나리오 | 기대 결과 |
|---|---------|----------|
| 1 | endMethod='camera' routine 알람 진입 → 카메라 마운트 + 미션 표시 | 정상 |
| 2 | 미션 대상 인식 → consecutiveHits 임계 → success 애니메이션 → handleDismiss → 다음 stage | 정상 |
| 3 | 인식 실패 → 사용자 재돌림 트리거 → MISSION_POOL 전체 풀에서 새 미션 픽 | 정상 |
| 4 | 카메라 권한 거부 → fallback UI (탭 dismiss) | 정상 |
| 5 | 콜드 스타트 (앱 kill 후 알람 응답) → RoutineAlarm 진입 → 카메라 마운트 fresh init | 정상 |
| 6 | 슬롯머신 재돌림 시 targetLabelsSV / thresholdSV 갱신 | 정상 |
| 7 | endMethod='camera' routine + AlarmScreen 카메라 모드 멀티 진입 충돌 X | 회귀 X |
| 8 | iOS / Android 양쪽 동작 (Expo Dev Build 필수) | 동일 |
| 9 | 4 stage modal (alarm → next → nextCountdown / auto / complete) 와 카메라 dismiss 흐름 호환 | 회귀 X |
| 10 | **분리 검증** — AlarmScreen `selectedMissions` 가 RoutineAlarm 진입 후에도 그대로 (변경 X) | 분리 OK |

---

## 8. 구현 창 전달문

```
[구현 창 전달용]
작업: v1.6 Phase 6.5 RoutineAlarm 카메라 모드 통합 (옵션 α + 분리 정책)

수정 파일:
  - src/screens/RoutineAlarmScreen.tsx (단일)
      Step A: placeholder L329-342 제거 + import 추가
        · useSharedValue from react-native-worklets-core
        · AlarmCameraMode from ./AlarmCameraMode
        · MISSION_COCO_LABELS, MISSION_CONFIDENCE_OVERRIDE from ../constants/missionIcons
      Step B: 부모-측 state/SV/애니메이션 신규
        · SharedValues 6개 (matched/lastRun/targetLabelsSV/thresholdSV/consecutiveHits/isShufflingSV)
        · 슬롯머신 state 3개 (isShuffling/shuffledList/shuffleIdx)
        · 슬롯머신 ref 2개 (shuffleIntervalRef/shuffleTimeoutRef)
        · UI 상태 / 애니메이션 / 레이아웃 (AlarmCameraMode props 요구치)
        · frame processor 갱신 useEffect — deps [cameraMission, targetLabelsSV, thresholdSV]
      Step C: AlarmCameraMode 마운트 + props 매핑
        · AlarmCameraMode props 인터페이스 정독 → 매핑 표 작성 → 누락 0 검증
      Step D: 슬롯머신 재돌림 + onMatchDetected + fallback
        · onReshuffle: MISSION_POOL 전체 풀 (selectedMissions 분기 X — 분리 정책)
        · onMatchDetected: handleDismiss() 재사용
        · fallback: 권한 거부=탭 dismiss / 시간 초과=isDanger 패턴 / 인식 실패=재돌림
      Step E: i18n 사이클 마지막 (ko)

수정하지 않는 파일 (분리 정책):
  - AlarmScreen.tsx (절대 변경 금지 — v1.5 회귀 방지)
  - AlarmCameraMode.tsx (365L 그대로 — props 인터페이스 변경 X)
  - missionIcons.ts (import 만)
  - AsyncStorage 'selectedMissions' 키 (read/write X — 분리)
  - ko.json / en.json (사이클 마지막)
  - Info.plist / app.json / babel.config / Podfile (v1.5 설정 그대로)

해당 레이어: UI + 로직 + 네이티브 API (vision-camera 권한)

주의사항:
  - 반복실수 #1: AlarmCameraMode props 매핑 시 SharedValue 타입 추측 금지 — 인터페이스 직접 정독
  - 분리 정책: selectedMissions / AlarmScreen 코드 절대 read/import X. 슬롯머신 풀 = MISSION_POOL 전체
  - AlarmScreen.tsx 절대 변경 금지 (v1.5 출시 기능 보호)
  - Expo Dev Build 필수 (Expo Go 동작 X)
  - 단계 분할: A → B → C → D → E 각 단독 적용 + tsc + Cmd+R + 보고

확인 방법:
  - iOS + Android Dev Build
  - 시나리오 #1~#10 (위 표). 핵심: #5 콜드 스타트, #7 멀티 카메라 진입 회귀 X, #10 분리 검증

배포 전제조건:
  - EAS Dev Build 재생성 (의존성 추가 없음 — v1.5 그대로 재사용 가능)
  - 권한 / 설정 변경 0
```

---

## 9. 메모리 (사용자 정책)

본 사이클 시점 추가된 메모리:
- [feedback_i18n_last.md](../../../../Users/taehwanum/.claude/projects/-Volumes-SeagateBac-moda-Timer/memory/feedback_i18n_last.md) — i18n 작업은 마지막 단계
- [feedback_timer_routine_separation.md](../../../../Users/taehwanum/.claude/projects/-Volumes-SeagateBac-moda-Timer/memory/feedback_timer_routine_separation.md) — 일반 타이머 vs 루틴 타이머 분리 정책

---

**작성:** 2026-04-27 플랜 창
**승인:** D-9 + D-10 사용자 승인 (2026-04-27)
**다음 단계:** 구현 창 인계 → Step A 시작 (사용자 명시 승인 후)
