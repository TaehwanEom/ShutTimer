node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*
.env
.DS_Store

# 루트 이미지 / 스크린샷
/*.jpeg
/*.jpg
/*.png
/*.PNG

# 빌드 산출물
*.ipa
*.apk
*.aab
build/
build */
# v1.7 hotfix #DBG-Sound — AppDelegate.swift 측 native 디버그 hook 영역 git 보존 영역.
# 영구 디버그 영역 (= 심사용 빌드 시 별도 제거). prebuild 측 overwrite 영역 측 본 파일 측 = 명시적 git 추적 영역.
# parent 디렉터리 (`ios/`) 측 통째 ignore 영역 측 = sub-file negation 영역 측 통과 ❌ → pattern 분해 영역.
ios/*
!ios/ShutTimer
ios/ShutTimer/*
!ios/ShutTimer/AppDelegate.swift
!modules/**/ios/

# 작업 자료 / 백업 / 개인 IDE 상태
.backup/
.claude/
*.mp4
sound/
svg/

# v1.8 — Android 빌드 산출물 (= prebuild 측 자동 생성. iOS 측 동일 패턴 정합).
android/
# 디바이스 디버그 로그
log.md
# Gradle 빌드 캐시
**/.gradle/
