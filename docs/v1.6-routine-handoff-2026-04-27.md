# v1.6-routine 인수인계 — 2026-04-27

## 즉시 결정 필요 (다음 채팅 첫 작업)

**진행 중 루틴의 편집 펜슬 시각 비활성화 + 우 스와이프 차단** — 사용자 마지막 요청.

명세:
- RoutineCard 의 편집 펜슬 `<Animated.View>` 를 `{!isActiveCard && (...)}` 로 조건부 렌더
- PanResponder `onPanResponderMove` cap: `Math.min(isActiveCard ? 0 : EDIT_SLIDE_WIDTH, g.dx)`
- 좌 스와이프 (휴지통) 처리 — **사용자 결정 대기 중** (편집만 차단? 삭제도 차단?)
  - 진행 중 routine 삭제 시 `ar` orphan 가능성 있음 — 함께 차단 권장
  - 단 사용자는 편집만 언급함

`handleEditPencil` 의 가드 (Alert 표시) 도 함께 추가 — defense-in-depth.

i18n 신규 (ko 만, 사용자 정책):
- `routine.editBlocked.title`: "루틴 진행 중"
- `routine.editBlocked.body`: "진행 중인 루틴은 편집할 수 없습니다. 정지 후 시도하세요."

---

## 본 사이클 완료 내역

### Fix A — HomeScreen `DeviceEventEmitter` listener (타이머→루틴 override 시 state 리셋)
- [HomeScreen.tsx#L522-L539](../src/screens/HomeScreen.tsx#L522): `timerCancelledExternally` listener
- 본문 — `notificationIdsRef.current=[]`, state/refs 8개 리셋, **`setSelectedMinutes(0)+setSelectedSeconds(0)+setSelectedIndex(-1)` 도 추가** (Fix D 포함)
- `cancelAlarms()` 호출 금지 — D-J race 차단

### Fix B — `handleStart` 진입 가드
- [HomeScreen.tsx#L432-L447](../src/screens/HomeScreen.tsx#L432): sync→async + `IS_ROUTINE_ACTIVE_KEY` 체크
- 루틴 진행 중이면 Alert + return
- i18n `home.timerBlocked.title/body` 신규 (ko + en)

### Fix C — Cold-start sanitization
- [HomeScreen.tsx#L540-L555](../src/screens/HomeScreen.tsx#L540): `Promise.all` 로 ACTIVE_TIMER_KEY + IS_ROUTINE_ACTIVE_KEY 동시 조회
- 루틴 active + stale timer storage → 자동 정리 + 복원 skip

### Fix E — `cancelBackgroundNotif` 강화
- [routineController.ts#L99-L110](../src/utils/routineController.ts#L99): 모듈 ref + iOS 시스템 큐 query
- `routine_chain` / `routine_confirm_prompt` 모두 cancel (강제 종료 후 stale notif 정리)
- `routine_prealert` 제외 (rolling schedule)

### Cold-start 이중 navigate 차단
- [App.tsx#L376-L382](../App.tsx#L376): setTimeout 진입 시 `currentRoute` 체크
- `RoutineList` / `RoutineAlarm` 이면 skip — 알림 탭으로 앱 진입 시 RoutineAlarm 2개 stack 회귀 차단

### Camera scan loop 차단
- [ActiveRoutineSection.tsx#L46-L49](../src/components/ActiveRoutineSection.tsx#L46): 모듈 레벨 `cameraScanNavigatedFor` flag
- [ActiveRoutineSection.tsx#L131-L143](../src/components/ActiveRoutineSection.tsx#L131): sig 비교 + currentRoute 체크 후 navigate
- sig: `routineId-startedAt-currentStepIndex` — step 진행 / 새 routine 시 재허용

### "다음 미션" → "다음 루틴" 푸쉬 텍스트 (ko 만)
- [ko.json#L320-L323](../src/locales/ko.json#L320): `chainTitle`, `chainBody`, `confirmPromptTitle`, `confirmPromptBody`
- [routineScheduler.ts#L431-L432, L474-L475](../src/utils/routineScheduler.ts#L431): defaultValue 동일 변경
- `prealertBody` 는 이미 "곧 루틴이 시작됩니다" — 변경 X
- en.json 미반영 (사용자 결정 — 영문 사용자는 ko fallback 받음)

### `'nextCountdown'` modal stage (5초 카운트다운)
- tap/shake/camera 모드의 'next' 모달에서 "다음 루틴 시작" 탭 시 5초 카운트다운 후 advance
- [ActiveRoutineSection.tsx#L43-L44](../src/components/ActiveRoutineSection.tsx#L43): `NEXT_COUNTDOWN_SEC = 5` 상수
- SVG ring 게이지 + 시각 카운터
- "취소" 버튼만 (정지 미노출)

### D-D 완료 — 4 stage 텍스트 i18n 통일
- 'alarm' / 'next' / 'auto' / 'complete' / 'nextCountdown' stage 의 하드코딩 13건 → `routine.run.*` 키 통일
- 'auto' 의 "진행됩니다" vs 'nextCountdown' 의 "시작됩니다" 의도적 분리

### shake 모달 "알람 종료" 버튼 제거
- [ActiveRoutineSection.tsx#L682-L699](../src/components/ActiveRoutineSection.tsx#L682): endMethod === 'shake' 일 때 "흔들어서 계속" 텍스트만, dismiss 버튼 미노출

### camera 완료 시 RoutineList 복귀 + tab 보존
- [RoutineAlarmScreen.tsx](../src/screens/RoutineAlarmScreen.tsx): 5개 `navigation.reset` 모두 `[Home, RoutineList, params={initialTab}]` 로 통일
- `getRoutineMode(routine)` 으로 tab 결정

### RoutineRun 화면 완전 제거
- `src/screens/RoutineRunScreen.tsx` 삭제 (git D)
- App.tsx 의 import + RootStackParamList + Stack.Screen 제거
- 콜드 스타트 navigate 'RoutineRun' → 'RoutineList' 변경

### ProgressBanner 제거
- RoutineList 상단 진행 중 빨강 배너 (ProgressBanner) + 관련 핸들러 (handleStop / handleBannerToggle / handleBannerPress) + activeRoutineObj state — 모두 제거

### 자동 스크롤 to active card
- RoutineList 의 cold-start 시 active routine 카드로 자동 스크롤
- `getInnerViewNode` Fabric 호환성 이슈 → public `measure` API 로 전환 ([RoutineListScreen.tsx#L771-L786](../src/screens/RoutineListScreen.tsx#L771))
- ScrollView wrapper View 의 `scrollContainerRef` + `scrollOffsetYRef` (onScroll 추적)

### 카드 자동 펼침 (cold-start 복원 시)
- [RoutineListScreen.tsx#L252-L262](../src/screens/RoutineListScreen.tsx#L252): `autoExpandedRef` guard — `activeRunNode` 첫 도착 시 1회 자동 펼침

### 마지막 step `'complete'` modal stage
- 자동 모드 마지막 step 완료 → 사운드 + 카운트다운 + "확인" 버튼 (사용자 명시 dismiss)
- 카운트다운 0 도달 시 사운드만 정지, modal 유지

### Audio session 옵션 복원
- [ActiveRoutineSection.tsx#L228-L233](../src/components/ActiveRoutineSection.tsx#L228): AlarmScreen 과 동일 — `playsInSilentModeIOS`, `staysActiveInBackground`, `interruptionModeIOS: DoNotMix`
- AdMob WebView 와 audio session 충돌 해결

### Swipe 임계값 조정 (휴지통/펜슬 양방향)
- [RoutineListScreen.tsx#L62-L70](../src/screens/RoutineListScreen.tsx#L62): 거리 1/3 + velocity 보조
- `TRASH_DISTANCE_THRESHOLD = 32`, `EDIT_DISTANCE_THRESHOLD = 18`, `VELOCITY_THRESHOLD = 0.25`
- `onPanResponderTerminationRequest: () => false` — ScrollView 의 vertical scroll 회수 차단 (lock 안 되던 진짜 원인)

### 펜슬 탭 시 swipe 리셋
- [RoutineListScreen.tsx#L461-L468](../src/screens/RoutineListScreen.tsx#L461): 편집 화면 진입 전 `translateX 0 spring` + `setSwipeRevealEdit(false)`
- 복귀 시 펜슬 노출 잔존 차단

---

## 미해결 / 별도 사이클 항목

### 푸쉬 알람 사운드 30초 한도
- iOS 시스템 한도 (~30초). 코드 문제 X.
- 사운드 파일 (`notification_alarm.wav`) 자체 29초 — 의도적 설계.
- 더 길게 재생하려면:
  - (a) 그대로 (현재)
  - (b) AlarmKit 으로 `routine_chain` / `routine_confirm_prompt` 마이그레이션 (iOS 26+ 한정, 작업량 ↑)
  - (c) Critical Alert 권한 (Entitlement 신청 필요)

### "(임시: 탭하여 계속)" 카메라 scan 힌트 문구
- [RoutineAlarmScreen.tsx#L331](../src/screens/RoutineAlarmScreen.tsx#L331) `cameraScanHint`
- `// TODO Phase 6.5: AlarmCameraMode 통합` — 정식 구현 전까지 유지 권장

### 'auto' / 'nextCountdown' 텍스트 의도적 분리
- "{n}초 후 다음 루틴이 진행됩니다" vs "시작됩니다" — 별도 키 유지 (의미 차이 보존)

### en.json i18n 미반영 항목
- `routine.confirmPromptTitle/Body` 등 영문 미추가. defaultValue (ko) fallback 사용.

---

## 핵심 사용자 정책

1. **i18n 변경은 ko 만** (사용자 명시): "루틴 푸쉬에 다음 미션이 시작됩니다" 변경 시 등.
2. **단계별 분할 적용 + 각 단계 Cmd+R 검증** (반복실수 #2 묶음 처리 회피)
3. **"확인/검토" = 보고만**. 실행은 별도 승인 후 (CLAUDE.md).
4. **사용자가 "진행해" / "적용" / 명시 승인 한 항목만 적용**.
5. **회귀 보고 시 코드 정독 → 명확 root cause + 1줄 fix**. 임계값 조정 같은 추측성 수정 금지.

---

## 검증된 시나리오 (현 시점)

| 시나리오 | 상태 |
|---|---|
| tap routine — 'alarm' → 'next' → 'nextCountdown' (5초 게이지) → 다음 step | ✅ |
| shake routine — 동일 흐름, dismiss 버튼 미노출 | ✅ |
| camera routine — RoutineAlarm 진입, 완료 시 RoutineList 복귀 | ✅ |
| auto routine 중간 step — 'auto' stage | ✅ (회귀 X) |
| auto routine 마지막 step — 'complete' stage | ✅ |
| 'nextCountdown' "취소" → 'next' 복귀, 재탭 시 5부터 재시작 | ✅ |
| 콜드 스타트 — active routine → RoutineList 자동 스크롤 + 카드 자동 펼침 | ✅ |
| 카드 swipe 좌/우 — 거리/속도 양쪽 임계 통과 시 lock | ✅ |
| 펜슬 탭 → 편집 화면 → 복귀 → 펜슬 잔존 X | ✅ |
| 타이머→루틴 override — Home state 리셋 (dial 0:00) | ✅ |
| 루틴→타이머 시작 시도 — Alert 차단 | ✅ |
| 콜드 스타트 stale state — 자동 정리 | ✅ |
| 강제 종료 후 재시작 — 푸쉬 1개만 발화 | ✅ |
| 카메라 RoutineAlarm 2개 stack 차단 | ✅ |
| RoutineList 재진입 시 camera scan loop 차단 | ✅ |

---

## 다음 채팅 시작 시 인수 받기

이 문서 + CLAUDE.md + git status 확인 후, 즉시 결정 필요 항목 (편집/삭제 차단) 부터 진행 권장.

git working tree 는 18개 파일 modified + 1 deleted. 커밋 미진행 (사용자 명시 외 자동 커밋 금지).
