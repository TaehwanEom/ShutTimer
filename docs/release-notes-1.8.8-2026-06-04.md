# App Store 업데이트 내용 (1.8.8) — 영·한·일·중 (2026-06-04)

App Store Connect "이 버전의 새로운 기능" 입력용. 그대로 복사해 사용.

> 범위: **오늘(2026-06-04) 수정한 내용만.** 워치는 이전 빌드에서 이미 심사 통과 → 제외.

---

## 한국어 (ko)
```
새로워진 기능
• 앱을 삭제했다 다시 설치해도 알람·루틴·설정이 그대로 복원됩니다.
• 루틴 단계 이름을 비워두면 '루틴 01', '루틴 02'처럼 자동으로 채워집니다.
```

## English (en)
```
What's New
• Your alarms, routines, and settings are now restored even after you delete and reinstall the app.
• Leave a routine step name blank and it fills in automatically (Routine 01, Routine 02, …).
```

## 日本語 (ja)
```
新機能
• アプリを削除して再インストールしても、アラーム・ルーティン・設定がそのまま復元されます。
• ルーティンのステップ名を空欄にすると、「ルーティン 01」「ルーティン 02」のように自動で入力されます。
```

## 简体中文 (zh-Hans)
```
新功能
• 即使删除并重新安装应用，您的闹钟、例程和设置也会自动恢复。
• 例程步骤名称留空时，将自动填入"例程 01""例程 02"等。
```

## 繁體中文 (zh-Hant) — 앱이 zh-Hant도 지원하므로 포함
```
新功能
• 即使刪除並重新安裝應用程式，您的鬧鐘、例程和設定也會自動還原。
• 例程步驟名稱留空時，會自動填入「例程 01」「例程 02」等。
```

---

## 포함된 변경 근거 (오늘 작업)
1. **삭제 후 재설치 복원** — iOS 실기기 검증 완료(온보딩 skip + 알람·루틴·설정 복원). 타이밍 레이스 수정 포함.
2. **루틴 단계 이름 자동 입력** — 미입력 시 "루틴 01~".
