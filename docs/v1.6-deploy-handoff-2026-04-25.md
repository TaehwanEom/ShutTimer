# v1.6 배포 창 핸드오프 — 2026-04-25 세션

**보내는 창:** 구현 창
**받는 창:** 배포 창
**브랜치:** `feature/v1.6-routine`
**세션 작업 범위:** 외장드라이브 이름 변경 영향 복구 + AlarmKit Swift 컴파일 fix + 미사용 ML Kit 의존성 제거

---

## ⚠️ 배포 가능 여부

**현재 상태로 프로덕션 배포 비권장.**

이번 세션은 **빌드 가능 상태 회복** 까지만 완료. AlarmKit 기능 자체는 **여전히 미완성 (직전 9fadc63 commit message 의 "미통합 (다음 작업)" 5개 항목 그대로 남음)**. v1.6 정식 출시 전에 별도 구현 + QA 사이클 필요.

이 핸드오프의 목적은 "현재 main 머지 가능 / TestFlight 내부 테스트용 빌드 가능" 수준 확인이지, App Store 제출 단계 아님.

---

## 본 세션 신규 커밋 (2건)

### `ff2cbac` — fix: v1.6 AlarmKit iOS 모듈 git 추적 누락 보완 + Swift 컴파일 fix

**핵심 변경:**
- `.gitignore` 수정 — 글로벌 `ios/` 룰 아래 `!modules/**/ios/` 예외 추가
- `modules/alarmkit-bridge/ios/AlarmkitBridge.podspec` 신규 git 추적 시작
- `modules/alarmkit-bridge/ios/AlarmkitBridgeModule.swift` 신규 git 추적 + 컴파일 fix 3건 적용 상태로 첫 커밋
  - L53 `AlarmButton(text:)`: `String → LocalizedStringResource(stringLiteral:)`
  - L58 `AlarmPresentation.Alert(title:)`: 동일 패턴
  - L83 `AlarmManager.shared.alarms`: throwing property `try` 추가

**근거:**
- Apple 공식 AlarmKit doc + `LocalizedStringResource(stringLiteral:)` 표준 패턴
- iOS 26 SDK 컴파일러 명시 지시

**위험도:** 낮음. 빌드만 통과 가능하게 한 mechanical fix. 동작 변경 없음.

### `af7e214` — chore: 미사용 ML Kit 의존성 제거 + v1-camera 보존 주석 정리

**핵심 변경:**
- `package.json`: `@react-native-ml-kit/image-labeling` 제거 + `expo.doctor` 예외 항목 정리
- `package-lock.json`: 1 package removed
- `src/screens/AlarmScreen.tsx`: `@preserve v1-camera` / `v1-ml-kit` 주석 + 비활성 import 라인 정리

**근거:**
- v1.5 부터 VisionCamera + YOLOv10 + TFLite 로 완전 전환되어 ML Kit 코드 사용 중지 상태
- ML Kit Pods (MLImage, MLKitCore 등 13개) 가 자동 설치되어 시뮬레이터 빌드 시 ld linker error 유발
- CLAUDE.md "보존 구역" 공식 목록에 ML Kit 미포함 (IAP 만 명시)

**위험도:** 낮음. 코드 차원에서는 사용 중지 상태였던 의존성 제거. 향후 복원 시 `npm i @react-native-ml-kit/image-labeling` 으로 즉시 가능.

---

## 빌드 / 검증 상태

| 항목 | 결과 |
|------|------|
| Pod install | ✓ 130 → 115 pods, EXCLUDED_ARCHS 경고 0건 |
| Swift 컴파일 | ✓ |
| Linker (이전 ML Kit 충돌 지점) | ✓ 통과 |
| 코드 서명 | ✓ |
| iPhone 17 Pro (iOS 26.4) 시뮬레이터 설치 | ✓ |
| **시뮬레이터 자동 launch** | ✗ **simctl helper 환경 이슈** (환경 문제, 코드/빌드 무관) |
| Metro bundler | 미확인 |
| AlarmKit `isAvailable()=true` 콘솔 확인 | 미확인 |
| AlarmKit scheduleAlarm 실제 발화 (Lock Screen / Dynamic Island) | 미확인 (실기기 필요) |
| iOS 25 이하 폴백 경로 회귀 (expo-notifications WEEKLY) | 미확인 |
| Android | 영향 없음 (no-op 골격 유지, 미확인 명시) |

---

## 외장 드라이브 이름 변경 영향 (참고용 — 코드 변경 아님)

세션 시작 시 `Seagate Bac` (공백 포함) → `SeagateBac` 으로 외장 드라이브 이름 변경되어 빌드 산출물에 옛 경로 잔여물 다수 발견. 다음과 같이 정리 (커밋 외 정리 작업):

- `ios/Pods/Target Support Files/Pods-ShutTimer/expo-configure-project.sh` 등 Pods 산출물: `pod install` 재실행으로 새 경로 반영
- `ios/build/generated/*` (RN codegen): 삭제 후 재생성
- `/Volumes/SeagateBac/.cache/xcode-deriveddata/ModuleCache.noindex/` (724M): 삭제
- `/Volumes/SeagateBac/.cache/xcode-deriveddata/CompilationCache.noindex/`: 삭제
- `/Volumes/SeagateBac/.cache/xcode-deriveddata/ShutTimer-cczyfuz...` (150M): 삭제
- `~/Library/Developer/Xcode/DerivedData/ShutTimer-abxfwvq...` (옛 경로 키): 삭제

**배포 창 영향:** 없음. 빌드 머신에서 이미 정상화된 상태로 빌드 가능.

---

## 미커밋 / 미추적 항목 (배포에 포함시키지 말 것)

```
.backup/AlarmScreen.tsx.pre-freeze-ux.bak
.backup/PoCPhotoValidationScreen.tsx.pre-freeze-ux.bak
.claude/
"ShutTimer_Review_Recording_v1.0.0.mp4 .mp4"
docs/plan-2026-04-19-v1.5-review-prep.md
docs/v1.6-progress-2026-04-25.md
docs/v1.6-deploy-handoff-2026-04-25.md  (본 문서)
sound/
svg/
```

모두 로컬 작업 자료 / 본 세션과 무관 / 별도 결정 필요. 배포 창은 무시.

---

## 배포 창 권장 진행

본 PR (2 commits) 만으로는 v1.6 정식 출시 불가. 아래 순서 권장:

1. **본 PR (`ff2cbac`, `af7e214`) 머지 후 dev TestFlight 내부 빌드 1회** — Swift 모듈이 다른 머신에서도 정상 빌드되는지 확인 (CI/EAS Build 머신 검증 목적)
2. AlarmKit 미통합 항목 별도 작업 사이클 (다음 섹션 참조) → 완료 후 QA 창 → 그때 정식 출시 빌드

---

## 빌드 / 환경 메모

- iOS 26.4 시뮬레이터 + iOS 18.5 시뮬레이터 보유 확인 (`xcrun simctl list runtimes`)
- Apple 개발자 서명 미확인 (실기기 빌드 시 Xcode 에서 Team 설정 필요)
- `app.json` `NSAlarmKitUsageDescription` 권한 문구 등록됨 (직전 9fadc63 에서)

---

**세션 종료 시점:** 2026-04-25
**작성자:** 구현 창
**다음 단계 계획:** [v1.6-progress-2026-04-25.md](v1.6-progress-2026-04-25.md) 참조 + 본 세션 종료 시 별도 next-steps 보고
