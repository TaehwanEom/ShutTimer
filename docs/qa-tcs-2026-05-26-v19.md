# QA Test Cases — ShutTimer v1.9 (2026-05-26)

**기준 인계**: `docs/handoff-deploy-2026-05-26-v19.md`
**대상 빌드**: v1.9 (자율주행 fix + v1.8.5 이후 누적)
**총 TC**: 25 (인계 Part 1 표 25개 시나리오 = A 7 + B 3 + C 6 + D 5 + E 4)
**총 commit**: 20 (인계 Part 6) + 21 (v1.8.5 이후 별도: 안드로이드 알람 엔진·Session 모델 통합) = **41**
**범위 주의**: TC-A1~E4는 후반 자율주행 fix 영역만 다룹니다. 안드로이드 알람 엔진 / Session 모델 통합 / iOS 알람 체인 견고화 영역은 별도 회귀 검증이 필요합니다 (아래 "추가 회귀 위험" 절).

---

## A. 알람/Timer 사운드 시스템

### [TC-A1] 알람 fire → 앱 active 진입 시 사운드 1초 끊김 없음
- Source: `343aa19` AlarmScreen Audio first then native stop (옵션 B)
- Platform: iOS
- Reproduction:
  1. 알람 등록 후 백그라운드 대기 → fire
  2. 앱으로 진입 (active)
- Expected: in-app 사운드가 native banner stop 전에 시작되어 1초 갭 없이 연속 재생
- FAIL: 진입 직후 1초가량 무음 구간 발생

### [TC-A2] chain max 30 = 60분 ringing 보장
- Source: `7e14942` chain 50 → 30 축소
- Platform: iOS
- Reproduction:
  1. 알람 등록
  2. dismiss 안 한 채로 native banner 발화 대기
- Expected: 약 60분 후 native banner 자동 종료 (chain 소진)
- FAIL: 60분 전 종료 또는 60분 초과 ringing
- Note: 60분 실대기 환경 없으면 UNTESTED

### [TC-A3] 루틴 진행 중 Timer 시작 시 갭 dialog 노출
- Source: `9b6e6db` 갭 체크 routine alarm 검사 추가
- Platform: Common
- Reproduction:
  1. 루틴 시작 후 step 진행 중
  2. Timer 화면에서 새 Timer 시작 시도
- Expected: 갭 충돌 dialog 표시
- FAIL: dialog 없이 새 Timer 즉시 시작

### [TC-A4] Timer 진행 중 routine 시작 시 갭 dialog 노출
- Source: `08a19c3` 갭 체크 timer_main 검사 추가
- Platform: Common
- Reproduction:
  1. Timer 진행 중
  2. 루틴 시작 시도
- Expected: 갭 충돌 dialog 표시
- FAIL: dialog 없이 루틴 즉시 시작

### [TC-A5] 갭 dialog 본문 = label만 표시
- Source: `c9f6593` dialog body 시각 표시 제거
- Platform: Common
- Reproduction: TC-A3 또는 TC-A4 발생 시 dialog 본문 확인
- Expected: "'XXX'이(가) 예정되어 있습니다" 형식 — 시각 표기 없음
- FAIL: dialog 본문에 시각(예: "22:14")이 포함됨

### [TC-A6] Timer 일시정지 + dial 회전 + 재생 → 갭 dialog 노출
- Source: `508564d` 갭 체크 함수 추출 + pause+dial 변경 측 갭 dialog
- Platform: Common
- Reproduction:
  1. Timer 일시정지
  2. dial 회전으로 시간 변경
  3. 재생 버튼 클릭
- Expected: 충돌 시 갭 dialog 표시 → "취소" 누르면 paused 유지
- FAIL: dialog 없이 진행 또는 "취소" 후 paused 깨짐

### [TC-A7] pause+dial dialog "취소" 시 isPaused 복원
- Source: `999b551` pause+dial dialog "취소" isPaused 복원
- Platform: Common
- Reproduction: TC-A6에서 "취소" 누르기
- Expected: UI 상태가 paused 그대로 유지 (카운트다운 진행 X)
- FAIL: "취소" 후 카운트다운이 진행됨

---

## B. 백그라운드 사운드 + orphan alarm cleanup

### [TC-B1] 백그라운드 routine step alerting = native banner 사운드만
- Source: `f4ecbde` routine in-app 사운드 background skip
- Platform: iOS
- Reproduction:
  1. 루틴 시작 후 앱을 백그라운드로
  2. step alerting 발화 대기
- Expected: native banner 사운드만 재생 (in-app 사운드와 2중 중첩 X)
- FAIL: 사운드 2중 중첩 또는 in-app 사운드만 재생

### [TC-B2] 잔존 옛 alarm = 앱 active 진입 5초 후 자동 정리
- Source: `f4ecbde` orphan cleanup on active
- Platform: iOS
- Reproduction:
  1. 옛 빌드 측 등록된 mapping 누락 alarm 잔존 상태
  2. 새 빌드 앱 active 진입
- Expected: 5초 후 cleanupGhostAlarms 자동 실행 → 옛 alarm 제거
- FAIL: 5초 후에도 옛 alarm 잔존
- Note: 잔존 alarm 환경 없으면 UNTESTED

### [TC-B3] 루틴 step 진행 후 다음 step alerting 정상 진입
- Source: `6d6469d` advance_done mapping save
- Platform: iOS
- Reproduction:
  1. 루틴 step 1 진행
  2. step 1 완료 → step 2 alerting 대기
- Expected: step 2 alerting 시 정상 화면 진입 (silent skip X)
- FAIL: step 2 alerting을 silent skip하여 화면 진입 안 됨

---

## C. soft delete + race fix 누적

### [TC-C1] 알람 dismiss 직후 잘못된 알람 fire 없음
- Source: `a12a56f` mapping table soft delete + listener deleted=true silent skip
- Platform: iOS
- Reproduction:
  1. 알람 fire → dismiss
  2. dismiss 후 1ms~30초 사이 관찰
- Expected: race window 안에서 잘못된 알람 fire 없음
- FAIL: dismiss 직후 옛 알람이 재발화

### [TC-C2] 포그라운드 알람 fire 시 listener race 없음 (정상 dispatch)
- Source: `dd75d20` listener cancelAlarm meta lookup 후 이동
- Platform: iOS
- Reproduction:
  1. 앱 포그라운드 상태
  2. 알람 fire
- Expected: AlarmScreen 정상 dispatch (listener race X)
- FAIL: dispatch 누락 또는 잘못된 화면 진입

### [TC-C3] 루틴 진행 중 위젯 일시정지 → LA pause 정상
- Source: `75e3eeb` Pause/Resume currentRunningAlarmId null fallback
- Platform: iOS
- Reproduction:
  1. 루틴 진행 중
  2. 위젯에서 일시정지 버튼 탭
- Expected: LA(Live Activity)가 pause 상태 진입 (silent skip X)
- FAIL: 일시정지 신호 silent skip, LA 상태 변경 안 됨

### [TC-C4] 옛 빌드 metadata 잔존 시 활성 once 알람 측 disable 안 됨
- Source: `9665106` CleanupAlertingAlarms full cancel + once chainBaseFireAt=null verify
- Platform: iOS
- Reproduction:
  1. 옛 빌드 측 metadata 잔존 상태
  2. 새 빌드에서 once 알람 등록
- Expected: 활성 once 알람이 cleanup에 의해 잘못 비활성화되지 않음
- FAIL: 등록한 once 알람이 cleanup으로 사라짐

### [TC-C5] 신규 알람 등록 직후 자동 cleanup 측 cancel 없음 (5초 delay)
- Source: `68f4316` soft delete ghost + read lock + cleanup delay + chain verify
- Platform: iOS
- Reproduction:
  1. 알람 신규 등록
  2. 등록 직후 5초 이내 관찰
- Expected: cleanupGhostAlarms가 신규 등록 알람을 5초 동안 건드리지 않음
- FAIL: 등록 직후 신규 알람이 cleanup으로 cancel됨

### [TC-C6] 알람 등록 rare 실패 시 native + mapping 일관성 보장 (rollback)
- Source: `9e698a0` scheduleAlarmAt atomicity rollback
- Platform: iOS
- Reproduction:
  1. 알람 등록 시 saveAlarmMetadata 실패 유도 (스토리지 가득 등)
- Expected: 실패 시 native alarm도 rollback되어 native/mapping 일관성 유지
- FAIL: native alarm은 등록됐는데 mapping에는 없거나 그 반대
- Note: 실패 유도 환경 어려우면 UNTESTED

---

## D. UI/State sync + StoreReview

### [TC-D1] 알람 dismiss 후 잘못된 화면 이동 없음
- Source: `461352c` dual SoT + StoreReview chain + timer persist + markRecommend await
- Platform: Common
- Reproduction:
  1. 알람 fire → dismiss
- Expected: 의도된 화면(Home 또는 RoutineComplete)으로만 이동
- FAIL: 엉뚱한 화면(SettingsTab 등)으로 이동

### [TC-D2] 미션 5회 성공 후 별점 모달 표시 + 중복 표시 없음
- Source: `461352c` StoreReview chain
- Platform: Common (production 빌드 필수 — debug는 EXPO_PUBLIC_HIDE_ADS 영향 X)
- Reproduction:
  1. 미션 5회 누적 성공
  2. 같은 세션 내 반복 검증
- Expected: 별점 모달 1회만 표시
- FAIL: 모달 미표시 또는 2회 이상 표시

### [TC-D3] 콜드 스타트 시 routine active 상태에서 timer stale cleanup 정확
- Source: `aff4602` HomeScreen cold-start Session SoT
- Platform: Common
- Reproduction:
  1. routine 진행 중 앱 강제 종료
  2. 앱 콜드 스타트
- Expected: HomeScreen이 Session SoT 기준으로 stale timer 정확히 cleanup
- FAIL: stale timer 잔존 또는 활성 routine이 잘못 cleanup됨

### [TC-D4] dial 회전 후 즉시 resume 클릭 시 새 endAt 정확 적용
- Source: `aff4602` persistPausedEndAt async await
- Platform: Common
- Reproduction:
  1. Timer 일시정지
  2. dial 회전으로 시간 변경
  3. 즉시 resume 클릭
- Expected: 변경된 endAt 기준으로 카운트다운 진행
- FAIL: 이전 endAt 기준으로 진행 또는 endAt 0/NaN

### [TC-D5] 알람 dismiss 후 새 알람 fire 시 layout 깨짐 없음
- Source: `e7b1476` AlarmScreen mount race mountedRef guard
- Platform: iOS
- Reproduction:
  1. 알람 1 fire → dismiss
  2. 즉시 알람 2 fire
- Expected: AlarmScreen이 정상 layout으로 렌더 (race 없음)
- FAIL: 빈 화면 / 깨진 layout / 중복 mount

---

## E. 캘린더/세션 + cache + 광고 + 로깅

### [TC-E1] 콜드 스타트 직후 알람 fire = dismissMethod 정확 적용
- Source: `5254027` preload await
- Platform: Common
- Reproduction:
  1. 앱 강제 종료
  2. 알람 fire (콜드 스타트)
  3. dismissMethod (흔들기/타이핑 등) 설정값 확인
- Expected: 사용자 설정 dismissMethod 그대로 적용 (default fallback X)
- FAIL: 흔들기 모드인데 typing default로 떨어짐

### [TC-E2] 같은 step 2회 advance = 캘린더 1회만 기록
- Source: `5254027` step/alarm dedup
- Platform: Common
- Reproduction:
  1. routine step에서 advance 신호 2회 연속 발생
- Expected: 캘린더에 1회만 기록
- FAIL: 캘린더 중복 기록

### [TC-E3] 사용자 알람 재시도 = 캘린더 30초 내 1회만 기록
- Source: `5254027` alarm record dedup
- Platform: Common
- Reproduction:
  1. 알람 fire → dismiss → 30초 내 재발화
- Expected: 캘린더에 1회만 기록 (30초 dedup window)
- FAIL: 30초 내 중복 기록

### [TC-E4] 백그라운드 진입 → 강제 종료 후 재시작 = 직전 로그 보존
- Source: `90c1242` logger flushLogs on background
- Platform: Common
- Reproduction:
  1. 로그 생성 후 백그라운드 진입
  2. 앱 강제 종료
  3. 재시작 후 로그 확인
- Expected: 종료 전 debounce pending 로그가 보존됨
- FAIL: pending 로그 손실

---

## 추가 회귀 위험 (v1.8.5 이후 별도 commit 21건 영역)

다음 영역은 인계 TC 범위 밖이나, v1.8.5 → v1.9 사이 변경이 있으므로 회귀 검증이 필요합니다.

| 영역 | commit 예시 | 권장 회귀 검증 |
|------|-------------|---------------|
| 안드로이드 알람 엔진 Phase 1 (모듈/예약/발화 체인) | `bd7677a`, `e2f715c`, `7dced55`, `3b9a5c4` | Android 측 알람 등록 → 발화 → 해제 풀 사이클 |
| 안드로이드 알람 사운드 P1/P2 (네이티브 단일 소스) | `1d2876c`, `96effea` | Android 측 알람 사운드 정상 출력 |
| 안드로이드 SafeAreaView 14개 화면 | `c22b3c7` | Android 측 상태바 겹침 없음 (14개 화면) |
| 안드로이드 MaterialIcons 임베드 | `244168b` | Android 측 아이콘 정상 표시 |
| 안드로이드 온보딩 레이아웃 | `690a311` | Android 온보딩 화면 정렬 |
| iOS 알람 체인 견고화 (A3) | `a3abcd0` | iOS 체인 멤버 전체 .relative 반복 등록 정상 |
| Session 모델 통합 v2.0 | `f741b6f`, `157d37c` | iOS/Android 양쪽 Session SoT 동작 |
| syncAllAlarms 멱등화 | `f82d3af` | 콜드 스타트 시 알람 체인 wipe 차단 |

---

## 결과 보고 format (antigravity_4_deploy.md 정합)

```
[Task] v1.9 QA 결과
Environment: Expo Go (debug) + Real Device (production)
Result: XX/25 PASS

| TC  | Scenario                     | iOS    | Android | Note |
|-----|------------------------------|--------|---------|------|
| A1  | 알람 fire 1초 갭 없음        | PASS   | -       |      |
| A2  | 60분 ringing 보장            | UNTESTED | -     | 60분 실대기 환경 X |
| ... | ...                          | ...    | ...     | ...  |

UNTESTED:
- TC-A2: 60분 실대기 환경 X
- TC-B2: 잔존 alarm 환경 X
- TC-C6: 실패 유도 환경 X

FAIL details (있을 시):
- TC-XX repro: ...
- Actual: ...
- Expected: ...
```

**판정 규칙**: PASS = 디바이스 직접 확인 + expected 일치. UNTESTED를 PASS로 마크하면 거짓보고. FAIL 1건 이상 시 Build 창 인계.
