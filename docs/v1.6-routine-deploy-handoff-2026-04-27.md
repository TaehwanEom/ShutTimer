# v1.6-routine 배포 창 인수 — 2026-04-27

**브랜치:** `feature/v1.6-routine`
**상태:** 미커밋 변경 다수 (커밋 분리 진행 필요)
**구현 창 작업 종료:** 2026-04-27

---

## 1. 인수 목적

`feature/v1.6-routine` 브랜치 working tree 의 미커밋 변경을 적절히 분리·커밋. 코드 수정 없음 — git 작업만.

---

## 2. 현 git 상태 (snapshot)

### 2.1 Modified (18) + Deleted (1)

```
M  App.tsx
M  modules/alarmkit-bridge/src/AlarmkitBridgeModule.ts
M  src/constants/categories.ts
M  src/constants/routines.ts
M  src/locales/en.json
M  src/locales/ko.json
M  src/screens/AlarmScreen.tsx
M  src/screens/HistoryScreen.tsx
M  src/screens/HomeScreen.tsx
M  src/screens/RoutineAlarmScreen.tsx
M  src/screens/RoutineCategoryScreen.tsx
M  src/screens/RoutineDaysScreen.tsx
M  src/screens/RoutineEditScreen.tsx
M  src/screens/RoutineListScreen.tsx
M  src/screens/RoutineSoundScreen.tsx
M  src/utils/routineController.ts
M  src/utils/routineScheduler.ts
D  src/screens/RoutineRunScreen.tsx
```

diff stat: 18 files changed, **1551 insertions(+), 978 deletions(-)**

### 2.2 Untracked — 분류 (배포 창 정정 2026-04-27)

**(A) 커밋 대상 — 본 사이클 신규 컴포넌트:**
- `src/components/ActiveRoutineSection.tsx` (핵심 신규 — RoutineList 인라인 진행 + 4 stage modal + audio session)
- `src/components/DurationWheelPicker.tsx`
- `src/components/TimeWheelPicker.tsx`

**(B) 커밋 대상 — 개발 문서:**
- `docs/plan-2026-04-19-v1.5-review-prep.md`
- `docs/v1.6-deploy-handoff-2026-04-25.md`
- `docs/v1.6-progress-2026-04-25.md`
- `docs/v1.6-routine-handoff-2026-04-27.md`
- `docs/v1.6-routine-deploy-handoff-2026-04-27.md` (본 문서, 정정 반영)

**(C) 커밋 제외 — .gitignore 추가 처리 완료:**
- `.backup/` (디렉토리 통째 ignore — `AlarmScreen.tsx.pre-freeze-ux.bak`, `PoCPhotoValidationScreen.tsx.pre-freeze-ux.bak`, `PoCPhotoValidationScreen.tsx.bak` 자동 처리)
- `.claude/` (개인 IDE 설정)
- `sound/` (정정: 앱 코드 미참조 grep 0건 — `sound/벨소리/99412B355CF6B93806.mp3` + `99D7364A5D542B0C0A.mp3` 보존, 실제 사용 음원은 `assets/sounds/`)
- `svg/` (정정: HTML 도구 파일 — `icon_selector.html` + `icon_selector 복사본.html`, 앱 번들 무관)

**(D) 처리 완료 — 외부 보존 + 워크 트리 제거:**
- `ShutTimer_Review_Recording_v1.0.0.mp4 .mp4` → `~/Desktop/ShutTimer-recordings/ShutTimer_Review_Recording_v1.0.0.mp4` 이동 (파일명 정상화: 공백 + 중복 `.mp4` 제거). `.gitignore` 에 `*.mp4` 추가.

---

## 3. 커밋 분리 제안

### 옵션 1 (권장) — 2개 분리

**커밋 1: v1.6-routine 본 사이클 통합 (14건)**
- 4-27 핸드오프 §"본 사이클 완료 내역" 14건 묶음
- 포함 파일:
  - 모든 modified (단 본 사이클 §3.2 i18n hunk 제외 — 분리 시 `git add -p` 필요)
  - `D src/screens/RoutineRunScreen.tsx`
  - 신규 컴포넌트 (A 분류): ActiveRoutineSection.tsx, DurationWheelPicker.tsx, TimeWheelPicker.tsx
  - 신규 자산 (확인 후): sound/, svg/

커밋 메시지 후보:
```
feat: v1.6 routine 사이클 통합 — Fix A~E + cold-start 차단 + camera scan loop fix + ProgressBanner 제거 + RoutineRun 삭제 + audio session 복원 등 14건

핵심 변경:
- HomeScreen: timerCancelledExternally listener + handleStart routine-active 가드 + cold-start sanitization
- routineController: cancelBackgroundNotif 강화 (chain + confirmPrompt 정리)
- App.tsx: cold-start 이중 navigate 차단
- ActiveRoutineSection 신설: RoutineList 인라인 진행 + 4 stage modal (alarm/next/nextCountdown/auto/complete) + camera scan loop sig 차단 + audio session 복원
- RoutineList: ProgressBanner 제거 + 자동 스크롤/펼침 + swipe 임계값 조정 + termination request 차단
- RoutineAlarm: navigation.reset 5건 통일 (RoutineList + initialTab 보존)
- RoutineRunScreen 삭제 (App.tsx import + RootStackParamList + Stack.Screen 제거)
- i18n: chainTitle/Body, confirmPromptTitle/Body 등 ko 변경 (en 미반영 — 정책)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**커밋 2: 본 사이클 — 진행 중 카드 편집/삭제 양방향 차단**
- 포함 파일:
  - `src/screens/RoutineListScreen.tsx` (본 사이클 hunk만)
  - `src/locales/ko.json` (editBlocked + deleteBlocked 4 키)
  - `src/locales/en.json` (editBlocked + deleteBlocked 4 키)
- ⚠️ ko.json/en.json 은 양 사이클 변경이 한 파일에 섞여 있음 — `git add -p` 로 hunk 분리 필요

커밋 메시지 후보:
```
feat: v1.6 진행 중 루틴 카드 편집/삭제 양방향 차단

- RoutineCard PanResponder cap (!isActiveCard) + dep + editAnim 가드
- isActiveCard 진입 시 swipe 자동 리셋 useEffect
- 휴지통 disabled + 펜슬 pointerEvents 가드
- handleDelete/handleEditPencil 부모 가드 (Alert) — defense-in-depth
- i18n: routine.editBlocked.{title,body} + routine.deleteBlocked.{title,body} (ko + en)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### 옵션 2 — 단일 커밋

`git add -p` 분리 부담 있을 시 양 사이클 통합. 메시지에 양 사이클 모두 명시.
권장도 ↓ — 진행 중 카드 차단은 명확히 분리 가능한 독립 작업이라 분리 가치 있음.

---

## 4. 사전 작업 (커밋 전)

1. **.gitignore 추가** (.backup/, .claude/) — 배포 창에서 검토 후 진행
2. **untracked 자산 검사** — sound/, svg/ 내용 확인 (앱에서 실제 사용되는 자산인지)
3. **이상 파일 처리** — `ShutTimer_Review_Recording_v1.0.0.mp4 .mp4` (파일명 + 대용량) — 사용자 확인 필요. 백업 후 워크 트리에서 제거 권장.

---

## 5. 검증 (커밋 후)

```bash
# 타입 체크
npx tsc --noEmit

# (선택) 빌드 무결성
npm run verify-build
```

본 사이클 종료 시점 `npx tsc --noEmit` EXIT=0 확인됨.

---

## 6. 다음 작업 (구현 창 인계 예정)

배포 창 커밋 완료 후 구현 창 인계:

**우선순위 (사용자 결정 2026-04-27):**
1. **T0 — Phase 6.5 카메라 모드 완전 구현** (다음 사이클)
2. T1 — AlarmKit 통합 (chain/confirm 마이그레이션)
3. T2 — 다단계 prealert (30분+5분)
4. T3 — 잠금 화면 컨트롤 (Live Activities / Foreground Service) — v1.7 권장
5. T4 — Phase 7 다국어 동기화

---

## 7. 주의사항

- **사용자 정책 #4** — 사용자 명시 외 자동 커밋 금지. 본 인수 문서 작성 자체는 사용자 명시 ("커밋 할거 배포창 전달") 에 따른 것.
- **CLAUDE.md 보존 구역** — 커밋 시 `@preserve IAP` / `@preserve` 표식 코드 삭제 금지 (purchase / IAP / location 등).
- **반복실수 #1** — 거짓/부정확 보고 금지. 커밋 후 `git status` 확인 + 미커밋 잔존 여부 보고.
- **반복실수 #5** — 본 인수 문서의 분류는 4-27 핸드오프 + git status 기준 snapshot. 배포 창에서 실제 hunk 진행 시 재확인 필요.

---

**작성:** 2026-04-27 구현 창 종료 시점
**다음 단계:** 배포 창에서 커밋 분리 진행 → 완료 보고 후 다음 사이클 (T0 카메라 모드) 구현 창 인계
